# pages
- 用途：定义 Astro 路由页面与文本端点。
- 关键入口：`index.astro`、`[slug].astro`、`okf-validator.astro`。
- 边界/依赖：页面只组合布局、组件和静态数据，不接后端服务。
> 一旦本目录内容变化，请更新本文件

## Files
- `[slug].astro`：普通内容页动态生成路由。
- `index.astro`：首页。
- `okf-validator.astro`：浏览器端 OKF 验证器。
- `robots.txt.ts`：生成 robots 文件。
- `sitemap.xml.ts`：生成 sitemap 文件。
- `llms.txt.ts`：生成短版 LLM 说明。
- `llms-full.txt.ts`：生成完整版 LLM 说明。
- `compare/`：对比页面路由。
